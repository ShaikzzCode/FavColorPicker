<resources>

    <!-- Base application theme. -->
    <style name="materialcolorpicker__AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="dialogTheme">@style/CustomAlertDialogPP</item>
    </style>

    <style name="CustomAlertDialogPP" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">#FF0000</item>
        <item name="android:textColor">#ffffff</item>
    </style>

</resources>
